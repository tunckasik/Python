# list of the names
names = ["MHT", "MNR", "NSB"]

names.append("ALI")

names.sort()

print(names)

#nested list
matrix = [[[k for k in range(4)] for j in range(3)] for i in range(2)]
print(matrix[:][:][:])